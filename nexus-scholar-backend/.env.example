# Database
DATABASE_URL="postgresql://nexus:nexus_secure_2024@localhost:5432/nexus_scholar"

# Redis (BullMQ queues)
REDIS_URL="redis://:redis_secure_2024@localhost:6379"

# Anthropic AI
ANTHROPIC_API_KEY="sk-ant-..."

# JWT
JWT_SECRET="generate-with: openssl rand -hex 64"
JWT_ACCESS_EXPIRY="15m"
JWT_REFRESH_EXPIRY="7d"

# App
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Email (Nodemailer)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM="NEXUS SCHOLAR <noreply@nexusscholar.com>"

# S3 / File Storage (optional)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET=nexus-scholar-docs
